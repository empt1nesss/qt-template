# Qt Template Project

If you on Windows you should specify path to your qt root directory in Makefile
and install MSVC2022 64 toolchain in qt installer.

To build your project you can use Makefile:
```sh
$ make conf   # to configure
$ make build  # to build
$ make run    # to run
```

